# v1 picks — the per-PR discipline (main-only runbook)

The standing maintenance of the two-edition delivery (ADR-0015 decision 2; spec `docs/specs/2026-09-11-delivery-versions-spec.md` § The Artifact Mechanism). `v1` is the booking-free artifact line seeded once by `git filter-repo` (#80); from that seed on, every PR squash-merged to `main` is triaged here — **pick**, **skip**, or **split** — executed, locked by CI + the private deploy + the export audit, and recorded in the ledger at the bottom. This file lives under `docs/agents/`, a path the seed removed from `v1`: it is main-only by construction and never enters the artifact (its own PRs are skips).

## The standing rule

**Main is a strict superset of `v1` by patch content.** Every commit on `v1` after the seed is either a cherry-pick of a main squash commit (carrying its `(cherry picked from commit …)` line), a split of one (that line plus a `Split:` line), or a revert of a pick. Nothing is authored on `v1` directly; main is never merged into `v1`; `v1` only ever moves by fast-forward pushes. Booking enters `v1` exactly once, as the paid v2 change-set — never through a pick.

Seed point: main `0bdfe82` ≡ `v1`'s rewritten `e314af3` (the seed content commit `0f4f340` sits on top). Everything merged to main after `0bdfe82` is triage backlog; the ledger starts there.

## The loop — on every squash merge to main

1. **Triage** (path pass + content pass, below) → PICK / SKIP / SPLIT.
2. **Execute** in the `v1` checkout — `~/Projects/sevendays-v1-seed`, branch `v1` (the seed clone kept as the working copy; any clone with `v1` checked out works). The main workspace never checks `v1` out.
3. **Lock**: local `pnpm check` + `pnpm build` green in the checkout, the export audit exit 0, then push; the push's run must show `check` + `Deploy v1 (private)` success and `Deploy teaser (main)` skipped.
4. **Record** the ledger row (this file, in a main PR — itself a skip).

Who runs it: whoever triages — agent or owner. The locks, not a reviewer, keep `v1` honest; anything the conflict policy calls ambiguous stops at the owner. Cadence: triage at merge time; a backlog is caught up in main order.

## Triage

### Path pass — `scripts/v1-triage.mjs`

```bash
node scripts/v1-triage.mjs <main-sha>
```

Classifies every path the squash commit touched as **MAIN-ONLY** (the seed's invert-paths list `scripts/seed-v1/paths.txt`, read live, plus the booking-cluster globs and the `docs/client/` fence) or **v1-path**, and prints the path-level verdict: all main-only → `SKIP`; none → `PICK`; both → `SPLIT`.

### Content pass — what never enters `v1` even on a v1-path

Read every v1-path hunk (`git show <sha> -- <path>`) against these rules; a hunk that trips one is dropped — and if every v1-path hunk trips, the PR is a SKIP:

- **Booking content** — anything carrying an absence-inventory token (`node scripts/audit-v1-absence.mjs --list-tokens`): the `RESEND_API_KEY`/`LANDING_ORIGIN` pair, the `resend` dependency, `appointmentQueries`, `createAppointment`/`getAppointment`, `phDateTime`, booking routes and links, the `/appointments` mount, `confirmation-email`. Also booking-coupled plumbing whose only purpose is the pair or the absent routes (test helpers that inject the pair, fixtures for absent endpoints). The audit is the mechanical backstop, not the first line.
- **Edition mechanics** — prose or config naming the editions, the teaser, the seed, picks, handover, `v1`/`v2`: `AGENTS.md`'s pick-discipline pointer, plan/progress prose, `docs/client/`. `v1`'s `AGENTS.md`, `docs/{PRD,architecture,tech-stack}.md`, ADR-0013/0016 and the CONTEXT glossaries are client-safe rewrites; only client-safe hunks may land on them.
- **Deployment identity** — `wrangler.toml`/`wrangler.jsonc` `name` fields and the `API` service binding stay `sevendays-v1-*` on `v1` (ADR-0016); a main hunk touching them is re-applied with the `v1` names.

Final verdict = the path verdict narrowed by the content pass. A real booking PR usually reads SPLIT on paths and SKIP on content (worked example #65 below).

### Transformed surfaces — pickable, expect conflicts

Files that exist on both branches with different content (the seed's booking-off rewrite): landing `components/{site-header,package-card,service-card,service-teaser-item,branch-card}.tsx`, `routes/{index,services}.tsx`, `lib/{queries,api.functions,format,format.test,api-404}.ts`; api `src/{routes/v1.ts,env.ts,env.test.ts}`, `package.json`, `wrangler.toml`, `.dev.vars.example`, `test/helpers/env.ts`; api-client `src/{index.ts,app-type.test.ts}`, `test/{mock-api,typing.test,loopback.test}.ts`; both frontends' `wrangler.jsonc`; `AGENTS.md`, `docs/{PRD,architecture,tech-stack}.md`, `docs/adr/{0013,0016}-*.md`, `CONTEXT-MAP.md`, `apps/*/CONTEXT.md`. Regenerables: `apps/landing/src/routeTree.gen.ts`, `pnpm-lock.yaml`.

## Executing a PICK

```bash
cd ~/Projects/sevendays-v1-seed
git switch v1 && git pull --ff-only origin v1
git fetch origin main
git cherry-pick -x <main-sha>          # conflicts → § Conflict policy
pnpm install --frozen-lockfile && pnpm build:packages && pnpm --filter @sevendays/api build
pnpm check && pnpm build
cd ~/Projects/sevendays && node scripts/audit-v1-absence.mjs v1 --repo ~/Projects/sevendays-v1-seed   # exit 0
cd ~/Projects/sevendays-v1-seed && git push origin v1
gh run list --branch v1 --limit 1 --json databaseId --jq '.[0].databaseId'     # then: gh run watch <id> --exit-status
gh run view <id> --json jobs --jq '.jobs[] | "\(.name): \(.conclusion)"'      # check: success · Deploy v1 (private): success · Deploy teaser (main): skipped
```

`-x` appends `(cherry picked from commit <main-sha>)` — the provenance line the superset check reads. The main SHA is absent from the export's history; accepted (ADR-0015 Consequences).

If the PR changed dependencies, `pnpm install --frozen-lockfile` fails or the lockfile conflicts (`v1` dropped `resend`): take `v1`'s lockfile (`git checkout v1 -- pnpm-lock.yaml`), run `pnpm install` to apply the PR's `package.json` changes, and commit the regenerated lockfile inside the pick — never hand-edit it. If the PR added or removed a landing route, regenerate the route tree on `v1` (`pnpm --filter @sevendays/landing generate-routes`) instead of resolving `routeTree.gen.ts` by hand.

## Executing a SPLIT

```bash
cd ~/Projects/sevendays-v1-seed
git switch v1 && git pull --ff-only origin v1 && git fetch origin main
git cherry-pick -n <main-sha>          # exits 1 when a main-only path is "deleted by us" (DU) — expected
git status --short                     # A = new main-only file, DU = absent on v1, M = v1-path hunk
git rm -qrf --ignore-unmatch -- <main-only path> [<main-only path> …]
git commit -F - <<EOF
$(git log -1 --format=%B <main-sha>)

(cherry picked from commit $(git rev-parse <main-sha>))
Split: main-only paths dropped — <path>, <path>
EOF
```

The blank line after the `%B` line is load-bearing: `$( )` strips `%B`'s trailing newlines, so without it a subject-only commit's provenance glues onto its subject line.

Then the same locks as a PICK (gates, audit, push, run). A v1-path hunk the content pass rejected (a booking-coupled edit to a shared file) is reverted before the commit with `git restore --staged --worktree --source=HEAD -- <path>` and named in the `Split:` line as `content-dropped: <path>`.

## Conflict policy on transformed surfaces

The pick reproduces the PR's **intent** on `v1`'s variant; it never restores booking or edition content. Per class:

- **Landing booking-off surfaces** (header, hero, cards, `/services` copy): keep `v1`'s call-forward content (spec § The Booking-Off Direction — "Call us" → `/branches`, `tel:` CTAs on branch cards, no CTA rows on catalog cards); re-apply the PR's styling, structure, and copy changes around it. A hunk whose substance *is* the booking CTA is dropped (the pick becomes a split).
- **api env-shed, api-client group, `routes/v1.ts` mounts**: the appointments/email entries never return; re-apply everything else.
- **`wrangler.*` configs**: `sevendays-v1-*` names and the `sevendays-v1-api` binding stay; re-apply other config intent.
- **Client-safe docs and `AGENTS.md`**: re-apply only client-safe, mechanics-free hunks — usually nothing.
- **Regenerables**: never resolved by hand — regenerate (route tree, lockfile) after the other hunks land.

Time-box: if a single pick's conflict work passes one hour, or the resolution amounts to re-authoring a surface rather than re-applying a change, stop — the owner decides, and the event counts toward the fallback trigger. Never resolve a conflict by weakening the audit or the checks.

## The locks — after every push

- `check` green on the `v1` run (`pnpm check` + `pnpm build` — the typed cascade fails any incomplete cut).
- `Deploy v1 (private)` success and `Deploy teaser (main)` **skipped** on that run — the pick touched only `v1`'s pipeline; the teaser lives on main's own runs.
- Export audit exit 0 over `v1`'s full history (run before the push; re-runnable any time).
- Live curl once the deploy leg lands: the picked content is served on `https://sevendays-v1-landing.pahamajulius.workers.dev`, `/book` is still 404 there, and the teaser still serves `/book` 200 — the deployed artifact is booking-free and the teaser is untouched by the pick.
- Superset check (any time): every `(cherry picked from commit <sha>)` on `v1` after the seed names a commit reachable from `origin/main` —

  ```bash
  cd ~/Projects/sevendays-v1-seed && git fetch origin main
  git log --format=%B 0f4f340..v1 | grep -o 'cherry picked from commit [0-9a-f]*' | awk '{print $5}' | sort -u \
    | while read s; do git merge-base --is-ancestor "$s" origin/main && echo "ok $s" || echo "NOT ON MAIN $s"; done
  ```

A red `check` on a pick: if the cause is content (the cut was incomplete), `git revert` the pick on `v1`, push, and re-triage — never force-push, never hot-fix on `v1`. If the cause is infrastructure (token, environment, worker), it is an owner-run ops fix; the pick stands.

## Fallback — the squashed snapshot

ADR-0015 holds one escape hatch if the discipline proves too costly: replace the lineage with a squashed snapshot of `v1`'s tree (absence trivially satisfied, provenance discarded). It is a one-way door — after clients hold `v1`-SHA repos, only another full handover can change the mechanism — so the trigger is explicit and the decision is the owner's.

**Trigger (owner-ratified 2026-09-11) — any one of:**

1. A single pick needs more than one hour of conflict work, or its resolution re-authors a transformed surface instead of re-applying the change.
2. `v1` lags main by more than 10 pickable PRs, or the oldest untriaged merge is more than 4 weeks old.
3. Two consecutive picks land red on `v1`'s `check` for content reasons.

A hit means: stop picking, record the hit in the ledger's notes, and bring it to the owner. The owner either rules a fix (usually: catch up in main order, or re-scope a surface) or invokes the fallback — which is its own ticket, never an in-loop action.

## Worked examples

- **#86 `fb92ef9` — SKIP.** The seed's own PR: `scripts/seed-v1/*`, `scripts/audit-v1-absence.mjs`, `docs/plan.md`, `docs/progress.md`, a plan file, `graphify-out/*` — 18 paths, all main-only. Nothing to read.
- **#65 `abfa9d2` (confirmation email + Resend; pre-seed, worked retrospectively) — SPLIT on paths, SKIP on content.** Path pass: 12 v1-paths (`pnpm-lock.yaml`, `apps/api/src/env.ts`, `env.test.ts`, `package.json`, `.dev.vars.example`, `wrangler.toml`, `test/helpers/env.ts`, five api test files) beside 29 booking-cluster and docs/graph main-only paths. Content pass: every v1-path hunk adds the `RESEND_API_KEY`/`LANDING_ORIGIN` pair, the `resend` dependency (the `pnpm-lock.yaml` hunk is its lockfile resolution entries), or routes the pair into test envs via `helpers/env` — nothing stands alone as booking-free value. Final: SKIP. (Its rewritten form already lives in `v1`'s history as `0c01531`; this is the example, not an executed skip.)
- **The route-titles PR (#88) — PICK.** Five landing route files, all v1-paths, no tokens; the first real pick (ledger).
- **The split drill — SPLIT.** A synthetic mixed commit (a v1-path comment + a booking-cluster edit + a new booking-cluster file), split on a local branch of the checkout, never pushed: `git diff --stat v1..HEAD` showed the v1-path only; the audit passed; both branches deleted.

## Ledger

One row per squash merge to main since the seed point, in main order. `v1 SHA` is the pick/split commit (— for skips). Drills are marked.

| Merged | PR | Main SHA | Verdict | v1 SHA | Notes |
|---|---|---|---|---|---|
| 2026-09-10 (pre-seed) | #65 | `abfa9d2` | skip (example) | — | booking PR worked retrospectively: paths SPLIT (12 v1-paths + 29 main-only), content pass drops all 12 (env pair / resend dep incl. its lockfile entries / helpers-env) → SKIP; rewritten form already in v1 as `0c01531` |
| 2026-09-11 | #86 | `fb92ef9` | skip | — | seed PR: ruleset + audit token fix + docs/graph — 18 paths, all main-only |
| 2026-09-11 | #87 | `f29ce87` | skip | — | #81 close-out: plan/progress ticks + plan file + graph — all main-only |
| 2026-09-11 (drill) | — | `d447716` (local) | split (drill) | `48ce4f3` (local, deleted) | synthetic mixed commit: about.tsx comment (v1-path) + booking.ts edit + new booking-drill.ts (booking cluster); classifier SPLIT 1+2; both main-only paths dropped (`A` + `DU`); footprint about.tsx only; audit PASS; never pushed |
| 2026-09-11 | #88 | `f256299` | pick | `8486a4c` | **first real pick** — per-route page titles, five shared landing routes; `-x` clean apply (5 files, +7); local gates + audit PASS; run 34624917515 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; titles live on sevendays-v1-landing, `/book` still 404, teaser untouched (no new main run) |
| 2026-09-11 | #89 | `296b986` | skip | — | this runbook + classifier + AGENTS.md pointer + plan/progress/graph — every path main-only; the AGENTS.md hunk is edition mechanics (content rule) — the next triager fills the SHA |
| 2026-09-14 | — | `87fe9e5` | split | `fbf8c3c` | direct docs commit, no PR: M3 design-system spec + ADR-0017 + AGENTS.md UI-rule amendment + plan/progress. Classifier SPLIT — 2 v1-paths (`AGENTS.md`, `docs/adr/0017-*.md`) + 3 main-only docs. Content pass: the three AGENTS.md hunks are client-safe (design-system prose, no edition tokens); ADR-0017 trips edition naming ("no shared-composed middle tier in v1") — scrub to "for now" when applying. Executed 2026-09-15 (backlog drain, second): AGENTS.md trio applied clean (client-safe design-system prose) + ADR-0017 created with the one edition trip scrubbed ("in v1" → "for now"); spec/plan/progress dropped (main-only). Locks: install/build:packages/api/check/build green (docs-only, turbo-cached), audit PASS 0/18; run 34986813933 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live landing 200, `/book` 404 |
| 2026-09-15 (backfill) | #91 | `fd94164` | split | — (reconciled empty) | missed at merge (2026-09-12) — no row existed; the 2026-09-14 session's close-out backfilled #86–#89 but not this. Classifier SPLIT — 10 v1-paths + 2 main-only. Content scan: two flags for the pick — tokens.css's PROTOTYPE comment "Dark mode is PARKED for v1" is design-version numbering, not edition vocabulary, but scrub on apply; the `/prototype-tokens` mock sidebar's "Appointments" string is display-only (not a booking affordance; audit tokens unaffected). Executed 2026-09-15 (backlog drain, first in main order): RECONCILED TO EMPTY — every substance hunk was pre-landed on `v1` by `22e89e7` in newer form (the #92 layer supersedes the interim teal ground; export entry + deps present); `prototype-tokens.tsx` + routeTree hunk dropped (gallery is main-only per the #103 ruling); progress/plan dropped (main-only); #91's interim `radix-rhea` components.json churn left for #102's `base-rhea` trio. Tree identical after resolution — no commit, no push, no locks needed |
| 2026-09-15 | #102 | `6f8f220` | split | `6552417` | M3 ticket 01 — packages/ui shared primitive library: components.json trio `base-rhea`/lucide/zinc, `button` on Base UI (`@base-ui/react`), `cn` package consolidation, check pipeline, tracer renders both apps, docs + graph. Classifier SPLIT — 20 v1-paths + 27 main-only. Content pass CLEAN on all 20 (only grep hits are lockfile integrity-hash substrings — noise). AGENTS.md hunk is design-system prose (client-safe, same ruling class as 87fe9e5's). Executed 2026-09-15 (backlog drain, third): `button` + packages/ui build-out (tsconfigs, lib/utils, components.json, biome) + the `cn` adoption (clsx/tailwind-merge out of both apps, hand-rolled utils deleted) + admin tracer; gallery Swatch tracer dropped (`prototype-tokens.tsx` absent on `v1`); styles.css @source hunks reconciled to v1's (pre-landed); lockfile regenerated per the transformed-lockfile procedure. Locks: check 35/35 + build 7/7 (packages/ui joins the pipeline), audit PASS 0/18; run 34987598277 green; live landing 200, `/book` 404 |
| 2026-09-15 | #103 | `4fe59cd` | split | `22e89e7` | M3 ticket 02 — token swap: the validated #92 logo-palette layer live in both apps, fonts self-hosted (Fontsource ×3), island palette removed, no chart tokens. Executed OUT OF main order (the three pending splits below were not yet run): this split pre-landed their styling-ground substance — #91's tokens.css + styles.css ground + the packages/ui `./tokens.css` export entry + `@sevendays/ui`/fontsource deps (families carry the `Variable` suffix), and #102's landing/admin `@source` line; both rows' executors must reconcile against `22e89e7` (expect near-empty style diffs). Gallery route + its 3 components dropped (seed-main-only); #102's `cn`/`radix-ui`/`tailwind-merge` adoption excluded (pending its own pick); `shadcn`(landing) carried because the picked styles import `shadcn/tailwind.css`. Locks: `pnpm check` 33/33 (after compose db up — env), `pnpm build` 6/6, audit PASS 0 hits/18 tokens; run 34917707145 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped |
| 2026-09-15 | #105 | `bd7a960` | split | `6e74bc6` | M3 ticket 03 — landing chrome + atmosphere: ink-led `SiteHeader`, new `SiteFooter` (no booking CTA by design — identical pre/post scrub), `MobileNav` disclosure panel, root-chrome mount (11 per-route header renders removed), home wash ground (hairline strips, white-card items, CoverPanel gradient, `data-strip='call-visit'` emphasis strip). Classifier SPLIT — 17 v1-paths + 18 main-only (`book.tsx`/`booking.$id.tsx` absent on `v1` → DU; docs/plan/progress/graphify-out main-only). Executed on main via SDD subagent pipeline (plan committed on the branch, 7 tasks, per-task + final MAX reviews PASS, controller evidence: CDP 16/16+12/12+15/15 unchanged, `pnpm check` 35/35, contrast 10/10, responsive/keyboard 11/11). **Pick NOT yet executed — order-gated:** the runbook's main-order rule puts `87fe9e5`, #91 (`fd94164`), #102 (`6f8f220`) ahead of it, and the chrome hard-depends on #102's `@sevendays/ui/components/button` + `cn` adoption (build breaks without). Conflict expectations for the executor: transformed surfaces are `site-header.tsx` (keep v1's call-forward CTA — panel/desktop CTA is "Call us" → `/branches` — inside the PR's ink-band structure), `mobile-nav.tsx` (panel CTA likewise), `index.tsx` (drop the hero "Book now" Link — substance IS the booking CTA; re-apply ground: hairlines/white cards/emphasis strip around v1's call-forward content), `package-card.tsx` + `service-teaser-item.tsx` (apply white-card/muted-text surface; DROP the "Book now" Links per seed's CTA-less cards), `services.tsx`/`branches.tsx`/`packages/*`/`about.tsx`/`__root.tsx`/`prototype-tokens.tsx` (header-removal hunks apply clean — wait: v1 may lack `prototype-tokens.tsx` entirely, same hazard as #91's row — create-whole-or-drop); `site-footer.tsx`/`nav.ts`/`collapsible.tsx`/`lib/nav` apply clean. No dependency changes (lucide/base-ui/cn arrive with #102's pick). Executed 2026-09-15 (backlog drain, last — BACKLOG NOW EMPTY): chrome landed with the conflict policy applied exactly as mapped — `site-header`/`mobile-nav` CTAs are "Call us" → `/branches` inside the ink-band structure; home hero keeps v1's ruled call-forward two-CTA row (Call Us + Services) re-skinned on `buttonVariants`; `PackageCard`/`ServiceTeaserItem` stay CTA-less with the white-card/muted-text surface applied; footer/nav/collapsible applied clean. Locks: check 35/35 + build 7/7 + v1 landing tests 15/15, audit PASS 0/18; run 34988517907 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live: "Call Us"+"Call us" served, ZERO "Book now", `/book` 404. Superset check: all provenance SHAs reachable from origin/main |
| 2026-09-16 | #100 | `026040e` | split | `c2c1056` | M3 ticket 06 — admin shell + IA (variant A) + the Tier-1 pull-list: variant A collapsible sidebar (Overview/Catalog/Studio, #59 ruling), sticky top bar, 7 flat-URL stub routes behind the `_shell` pathless layout — the dashboard the honest v2-stub empty state and stub copy naming M4/M5/v2 (#93-ruled product content, picked), Toaster mounted, form substrate (`@tanstack/react-form` + zod-4 Standard Schema; the zod-3-only adapter NOT installed), the full Tier-1 pull-list into `packages/ui` (+ transitive `tooltip`/`use-mobile`; deps `sonner`/`next-themes`/`lucide-react`/`@tanstack/react-form`). Classifier SPLIT — 41 v1-paths + 22 main-only (docs/plan/progress/superpowers-plan + graphify-out, dropped). Content pass: `AGENTS.md` hunk content-dropped (edition-referencing ui bullet), `apps/admin/README.md` content-dropped (milestone-naming prose). Lockfile regenerated per the transformed-lockfile procedure (admin/ui deps in; `resend` stays out). **Audit instrument ruling (owner, 2026-09-16):** the `'/appointments` token (quoted-literal form, shaped for the API mount) false-hit the shell's ruled stub literals (nav `to:`, routeTree) — reshaped to `.route('/appointments'` (call form; the mount stays triple-covered with `routes/appointments` + `/api/v1/appointments`); self-test green; the reshape lands with this row. **Provenance repair:** the first pick (`334eb78`) mistyped the main sha in its provenance line (hand-written, not `rev-parse`d) — reverted in `164f77f` and re-landed as `c2c1056` with the true sha; `334eb78`'s message permanently carries the bogus line, so the superset check flags it by design (`FLAG 026040ef16e5…` = the documented typo, not a real divergence). Locks: check 35/35 + build 7/7, audit PASS 0/18 post-reshape; run 34996115915 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live landing 200, `/book` 404, zero "Book now" served |
| 2026-09-16 | #109 | `d6d507e` | skip | — | docs-only: ledger row for #100 + audit token reshape — docs/agents/v1-picks.md + scripts/audit-v1-absence.mjs, both main-only (classifier) |
| 2026-09-16 | #110 | `8c89d63` | split | `df1631e` | M3 ticket 04 + #111 remediation — **executed 2026-09-20 (#101's backlog drain)**: Treatment B chrome + F1 fix + defect-list polish + badge/popover/db-state/ui-bullet hunks picked; booking CTA hunks + the whole prototype/D surface + photos + scripts/docs dropped per the pre-mapped conflict expectations (row above). Locks: check 35/35 + build 7/7, audit PASS 0/18; run 35482738934 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live: landing 200, `/book` 404, call-forward CTAs served, zero "Book now". Photos + variant D home deferred to #101's own pick |
| 2026-09-16 | #112 | `7d93829` | skip | — | docs-only: ledger row for #110 (pending) + progress #111 close-out — docs/agents/v1-picks.md + docs/progress.md, both main-only (classifier) |
| 2026-09-20 | #113 | `d775a2c` | skip | — | #99 booking-flow componentization — all 28 paths main-only (booking cluster + scripts/ + docs/graph; classifier). Mechanical skip; CDP/e2e evidence rode the ticket |
| 2026-09-20 | #101 | `707120b` | split | `a59798d` | M3 ticket 07 — M3 verification + close-out — **executed 2026-09-21 (post-merge, the close-out loop's tail)**: the variant D composition applied whole with v1's ruled call-forward scrub — hero CTAs `Call Us` → `/branches` + `Services` → `/services` (blurb/kickers/microcopy verbatim), catalog cards CTA-less (`BookNow` + its `buttonVariants` import dropped), service-card affordance retargeted `/book?service=` → `/services` (chip `Book` → `View`); serif register + `public/photos` landed whole (photos supplemented from 707120b — the squash's diff predates them); main-only docs/graph/`scripts/verify/content-pages.mjs` dropped (prototype paths = deletion hunks for files v1 never had, DU no-ops). Locks: check 35/35 + build 7/7, audit PASS 0/18; run 35595987551 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped. Live: `/book` 404, zero "Book now"/`>Book<`, variant D home byte-verified across healthy windows (hero line + gallery strip + ratified blurb + Call Us pair all present) — intermittent 500s/latency during a Supabase API-Gateway degradation window (status page; BOTH editions affected, main's API equally slow; the pick touches no API/DB code). Follow-up once Supabase settles: re-run the four curls and mint the two post-pick home shots (`git show d775a2c:apps/landing/scripts/prototype-shot.mjs > /tmp/prototype-shot.mjs` then `node /tmp/prototype-shot.mjs https://sevendays-v1-landing.pahamajulius.workers.dev/ .scratch/101-closeout-shots/v1/home-variant-d.png` [+ `390 2400` for mobile]) |
| 2026-09-22 | — (direct docs commit) | `6aff692` | split | `e75bddd` | Post-M3 docs-alignment pass (2026-09-22 audit catch-up): progress.md rotated to M4 + the closed #21 bullet moved to What Exists + dist path fixes; architecture.md packages/ui bullet + frontend-dependency sentence, tech-stack.md shadcn line, AGENTS.md primitive count re-aligned to ADR-0017; PRD.md V1 Scope delivery-editions annotation. Classifier SPLIT — 4 v1-paths (`AGENTS.md`, `docs/{PRD,architecture,tech-stack}.md`) + `docs/progress.md` main-only. Content pass: the AGENTS/architecture/tech-stack hunks are design-system prose (client-safe — the `87fe9e5` ruling class; no edition tokens); the PRD annotation trips edition vocabulary (v1/v2, spec #76, ADR-0015) — content-dropped at apply. **Executed 2026-09-22 (#115 session):** AGENTS/architecture/tech-stack applied (AGENTS.md count-hunk resolved to the incoming 22-primitive wording — true on v1 post-#100-pick), PRD annotation content-dropped, progress DU. Locks: gates FULL TURBO (check 35/35 cached, docs-only), audit PASS 0/18; run 35676585212 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live landing 200, `/book` 404, call-forward CTAs served, zero "Book now" |
| 2026-09-22 | #115 | `6ceee8f` | split | `8c133b6` | architecture.md alignment pass — diagram's `API` service-binding production-transport label + the branch-keyed-CI Deployment Targets table + the tech-stack § Continuous deploy pointer applied clean (v1's diagram/table were byte-identical to main's pre-#115 state, and Task 5's `e75bddd` pick had already pre-aligned the intro sentence + packages/ui bullet context); Data Flow + Observability hunks content-dropped (booking content — the `/booking` + `/api/v1/appointments` strings are audit tokens; v1 keeps its Catalog Reads variant + its own client-safe analytics line); `docs/progress.md` + the SDD plan file main-only. Classifier SPLIT — 1 v1-path + 2 main-only. Conflict resolution pre-mapped in the #115 plan (Task 6). Locks: gates FULL TURBO (check 35/35 cached, docs-only), audit PASS 0/18; run 35677096372 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live landing 200, `/book` 404, call-forward CTAs served (2), zero "Book now" |
| 2026-09-22 | — (direct docs commit) | `a221d2c` | split | `e44b744` | Better Auth skills/docs guidance pass (SKILL.md integration + versioning guidance, `.zcodeignore`, AGENTS.md's Better Auth section). Classifier SPLIT — 2 v1-paths (`.zcodeignore`, `AGENTS.md`) + 5 main-only (4 SKILL.md files, skills-lock.json), dropped. Content pass: the AGENTS hunks are docs-tooling prose — client-safe; the `v1.7`/`v1.6` strings are BetterAuth DOC versions, not delivery editions. AGENTS.md conflict resolved to main's section verbatim (byte-fidelity diffed against 707c98a:AGENTS.md — PASS). Locks: check 35/35 + build 7/7, audit PASS 0/18; run 35694858130 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped. Row owed from the prior session (the commit sat unpushed on local main and merged to origin as part of #123's squash — recorded here in main order, before #118's row) |
| 2026-09-22 | #118 | `707c98a` | split | `70ba992` | M4 ticket 01 — BetterAuth foundation: `better-auth@^1.7.5` in both apps (+ admin gains `@sevendays/db`), the config-pair `apps/admin/src/lib/auth.ts` (factory) + `auth.config.ts` (CLI instance — the 1.7.5 CLI reads an exported instance, not a factory), the generated auth schema (5 tables) + relations + barrel, migration 0005 files, dev env examples, the truncate-helper quoting + 18-table pin, both CI legs' BETTER_AUTH_URL --var + admin DATABASE_URL sync. Classifier SPLIT — 16 v1-paths + 26 main-only (docs/plan/progress/spec/plans + graphify-out, dropped). Content pass: `.dev.vars.example` resolved to v1's booking-free variant (incoming resend/`LANDING_ORIGIN` blocks DROPPED, BetterAuth block kept); lockfile regenerated per the transformed procedure; migration files carried WITHOUT re-migrate (editions share the live DB — 0005 applied once on main's merge). Locks: check 35/35 + build 7/7, audit PASS 0/18; run 35694858130 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live: landing 200, `/book` 404, `v1-api /api/v1/appointments` 404-by-absence. Live secret/var verification (BETTER_AUTH_SECRET puts, login-on-v1) = #122's gate |
| 2026-09-22 | #119 | `ee8705c` | split | `624b424` | M4 ticket 02 — admin auth server live: `/api/auth/$` catch-all (`server.handlers` GET/POST → `auth.handler` per request, ADR-0011), `auth.functions.ts` session fns, admin `create-staff` script line, regenerated admin routeTree. Classifier SPLIT — 4 v1-paths + 20 main-only (docs/plan/progress/superpowers-plan + graphify-out, dropped). Content pass: `docs/staff-provisioning.md` classified v1-path but CONTENT-DROPPED (edition mechanics — #75 handover / v1-machinery / ticket references; v1 carries the create-staff script without the main-only doc). Executed 2026-09-22 (this ticket's session): cherry-pick -n applied the 4 v1-path hunks clean (routeTree hunk carried footer-less per main's ruling — accepted canonical there too). Backlog reconciliation first: `f3f712e` never merged to main (orphaned local commit; its spec content reached origin/main via `707c98a`/#123 which has the row) — no row owed. Locks: install/build:packages/api-build/check/build green (35 tasks; admin#test no-op warning = known noise), audit PASS 0/18; run 35725824182 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live landing 200, `/book` 404. Note: the shared dev database already carries the staff row (owner@sevendays.test) — the pick carries no data, no migration ran; nothing can sign in on v1 until #122's secrets + #120's UI |
| 2026-09-23 | #120 | `95f0041` | split | `199aad7` | M4 ticket 03 — login UI + `_shell` session gate: `auth-client.ts` (browser client, whole), `login.tsx` (owner-ruled hybrid composition, whole), `_shell.tsx` `beforeLoad` gate + identity-in-context, sidebar user card (identity + Option-2 inline sign-out), routeTree `/login` hunk (footer-less, applied clean). Classifier SPLIT — 5 v1-paths + 17 main-only (docs/plan/progress/superpowers-plan + graphify-out, dropped). Content pass clean (the login headline's "appointments" is marketing prose, not a token; the routeTree `/appointments` lines are pre-existing v1 context). Executed 2026-09-23: seed routeTree vite-footer drift restored before the pick (same T1-R1 class as main); cherry-pick -n applied the 5 v1-path hunks clean. Locks: install/build:packages/api-build/check/build green, audit PASS 0/18; run 35804739300 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live landing 200, `/book` 404. Carries the final-review correction: better-auth 1.7.5's client sets `credentials: "include"` itself (`config.mjs:39`) — fixed in the comment before the pick. Note: v1's `/login` renders but cannot authenticate until #122's v1 secret puts (the pick carries no data; shared dev DB holds the staff rows). |
| 2026-09-23 | #121 | `e829181` | split | `bd066eb` | M4 ticket 04 — API session verification + the appointments-list gate: `services/auth.ts` (createVerificationAuth + requireSession + SessionData, whole), `test/helpers/auth.ts` (test issuer, whole), `test/require-session.test.ts` (7 scratch-app tests, whole; resolves `acquireDb` from v1's own `routes/v1.ts`); hunks: `env.ts`, `env.test.ts`, `services/db.ts` (`session?` Variables, type-only), `test/helpers/env.ts` (`TEST_AUTH_SECRET`), PLUS `apps/admin/src/lib/api.server.ts` — the classifier's +1 v1-path beyond the plan's 7-path pre-map (the `getSessionScopedApiClient` seam is generic auth plumbing, zero booking tokens; carried whole-hunk). Classifier SPLIT — 8 v1-paths + 19 main-only (appointments route + test absent on v1 → DU; docs/plan/progress/superpowers-plan + graphify-out, dropped). Audit-token fence verified at plan time (Task 2 grep gate) and pick time (zero appointments strings in the three carried files). Conflict pass on v1's booking-free env variant: the email pair (RESEND_API_KEY/LANDING_ORIGIN) stays out of `env.ts`/`test/helpers/env.ts` (comments re-spliced around the secret prose); the auto-merged `env.test.ts` initially carried main's 3-key parseEnv calls — first audit run FAIL 2/18 tokens caught it, the two new env tests adapted to v1's DATABASE_URL(+optional secret) binding, pick amended `9ff36f7`→`bd066eb`, audit PASS 0/18. No lockfile change (no deps moved). routeTree vite-footer drift restored post-build (T1-R1 class, #120 ruling). Locks: install/build:packages/api-build/check/build green (check 35/35, build 7/7; admin#test no-op warning = known noise); run 35813098027 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live: landing 200, `/book` 404, `v1-api /api/v1/appointments` 404-by-absence (nothing to gate until M5 mounts CMS routes behind the now-tested middleware). Live BETTER_AUTH_SECRET puts + the authenticated read through the seam = #122's gate. |
| 2026-09-23 | #127 | `d236495` | split | `3c87820` | M4 ticket 05 — live verify + docs rotation (milestone close): progress session record + M5 next steps, tech-stack Auth integrated posture, AGENTS auth-state flip (migrations 0000–0005), CONTEXT Session/Staff User, plan.md M4 ticks. Classifier SPLIT — 4 v1-paths + 16 main-only (progress/plan/plan-file/graphify, dropped). Content: tech-stack + api CONTEXT auto-merged clean; AGENTS.md + admin CONTEXT conflicted (v1 lacks main's anchors) — adapted per the client-safe-docs class: AGENTS auth-state flip landed on v1's own status line ("not wired in" → integrated), staff-provisioning.md refs scrubbed to the create-staff CLI, Fulfillable/Dashboard context not carried. Locks: check 35/35 + build 7/7, audit PASS 0/18; run 35885836563 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live: landing 200, `/book` 404, v1-admin ok probe 200. The ticket's live gate itself (secret repair, teaser 401/200/revocation, v1 login + cross-edition token check) is recorded in progress.md — main-only by design |
| 2026-09-24 | — (direct) | `8142d41` | split | `9f61113` | ADR-0018 — two-role staff model (owner holds `admin`, staff hold `staff`; interim `--role staff` runbook rule, spike-verified): +admin CONTEXT role sentence, +staff-provisioning.md role section, progress.md pointers. Classifier SPLIT — 3 v1-paths + 1 main-only (progress, dropped). Content: ADR-0018 applied clean; admin CONTEXT conflicted on the M4-pick scrub (v1 line lacked the runbook ref) — resolved by taking the incoming wording (the ref is valid again: this commit also creates `docs/staff-provisioning.md` on v1 with the dangling M4-spec pointer scrubbed — closes a #119-pick gap, the file is v1-classified but had never landed). Locks: check 35/35 + build 7/7, audit PASS 0/18; run 35891717833 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live: landing 200, `/book` 404, ok probe 200 |
| 2026-09-25 | #144 | `1bbb568` | split | `2b3ba16` | M5 ticket 01 — catalog schema + shared types: migrations 0006/0007 (gallery/testimonials tables, `is_active` on the three lookups, `position` on inclusions + junction; carried WITHOUT re-migrate per the #118 precedent — editions share the live DB), catalog-rows builder positions, `packages/types` write-model vocabulary (frame-token atomic package save, presence-encoded media inputs per ADR-0019, matrix + order payloads, `coverImageUrl`/`photoUrl` reads; dead pre-M5 create schemas deleted), api +19 schema-contract tests (103→122), landing `isActive` fixture fix (zod default → output-required). Classifier SPLIT — 46 v1-paths + 26 main-only (docs/plan/progress/spec/plans + graphify-out dropped; the two booking-cluster landing lib tests' hunks dropped — `bookable-branches.test.ts` is a v1-path and carried). Content pass: the migration snapshot JSONs carry appointment TABLE identifiers (pre-existing pattern — editions share the schema; the audit is the backstop); AGENTS.md conflict resolved to v1's own status line carrying the migration-count fact (the incoming M4-closed prose stays main-only per #127's client-safe-docs class). Locks: install/build:packages/api-build green, check 35/35 + build 7/7, audit PASS 0/18; run 36167185961 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped; live: v1 landing 200, `/book` 404, teaser 200. Note: three #133 docs commits sat unpushed on local main; the squash carried their content byte-identically (verified) and local main was reset to origin/main — the `a221d2c` precedent. |
| 2026-09-26 | #145 | `f3d910d` | split | `cc3ed71` | M5 ticket 02 — media foundation: the ADR-0019 pipeline executable + provisioned — `packages/types` media vocabulary (presign request/response + staging-key shape), the media service (`presignUpload` with Content-Type signed via aws4fetch `allHeaders: true`; `commitUpload` HEAD-verify → caps → immutable promote → delete staging, the foreign-key gate refusing non-`tmp/` keys WITHOUT delete), the gated `/api/v1/admin` sub-app root (ONE `requireSession`) + presign + by-id thumb routes (width-400 webp, >20 MB original fallback), env schema (bindings + `CLOUDFLARE_ACCOUNT_ID`/`MEDIA_PUBLIC_BASE_URL` required, R2 S3 pair optional/loud), `aws4fetch@1.0.20`, CI `--var` legs + fail-soft S3 sync on both deploy jobs, runIf-gated LIVE harness (3 skips), api 122→153 (+3 skips), types 105→112. Classifier SPLIT — 21 v1-paths + 3 main-only (docs/plan/progress/superpowers-plan, dropped). Content pass: `docs/media-bucket-runbook.md` content-dropped (owner runbook, deployment-identity content — names both Workers' origins); AGENTS.md media sentence dropped (dangles the main-only runbook path). Conflicts: booking-off shed re-applied on the transformed env trio (no `RESEND_API_KEY`/`LANDING_ORIGIN` — the email-pair prose and 4 env tests shed, 19→15 env tests) and `routes/v1.ts` (main's hunk bundled the admin import WITH the appointments import — admin kept, appointments shed); `wrangler.toml` binding blocks applied under `sevendays-v1-api` (one-hunk conflict, name preserved); `pnpm-lock.yaml` regenerated on v1 (aws4fetch only). Locks: install/build:packages/api-build green, check 35/35 + build 7/7 (admin#test no-op warning = known noise), audit PASS 0/18; run 36185464444 `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped. Live curl verification deferred to the owner's S3-token mint (the runbook's § Live round-trip verify drives the real bucket on both editions; bucket-level config is edition-shared by construction — ADR-0019 #5). |
| 2026-09-26 | #146 | `abd296b` | split | `d882608` | M5 ticket 03 — admin write model: the gated `/api/v1/admin` write surface over one `requireSession` — `services/admin-shared.ts` (the resolved-union failure contract, the `23505` constraint→field mapper with the drizzle `err.cause` unwrap, `guardUnique`, `AdminSaveError` thrown inside tx for real rollbacks), nine entity inventories (full-object POST 201 / PUT 200, `isActive` flips, no deletes), the ATOMIC package save (array order = frameNumber/position, children fully rewritten per save, cover presence-encoded through #136's `commitUpload` — old object deleted only after commit), the two matrix full-replaces + three total order PUTs (single-tx, per-id 400 details), slug lifecycle (generate/format/uniqueness, old slug public 404), `resolveMediaUrl` + `assemblePackageRead` export seams, `r2Key`/`coverImageKey` stripped server-side, api 153→262 (+3 gated skips). Classifier SPLIT — 26 v1-paths + 3 main-only (docs/plan/progress/superpowers-plan, dropped). Content pass: AGENTS.md media sentence content-dropped (dangles the main-only runbook path — #145 ruling consistency); zero audit tokens in new content (bare "appointments/booking" comment words are not inventory tokens — audit is the backstop). Cherry-pick clean (the #145-picked api contexts matched); CAUGHT: the squash also carried the 137 plan doc as an added path — amended out (25 files final). Locks: install/build:packages/api-build green, check 35/35 + build 7/7, audit PASS 0/18; run 36201730765-era `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped. Deferred minors ledgered in the SDD workspace (uncovered PUT pre-check arms → #138-adjacent sweeps; zod `.default(true)` reactivation fence → #139 client discipline). |
| 2026-09-26 | #147 | `14c89cc` | split | `ebe85f6` | M5 ticket 04 — public reads + trim rules: the public package reads reworked (`fetchPublicComposition`/`toPublicRead` — trim rules verbatim: attireless inclusion renders, sizeless hides on non-null printSizeId (AR1), all-trimmed lists with `inclusions: []`; `(position, id)` ordering; `ServicePackageRead` wire with `coverImageUrl` resolved, raw keys stripped), the api-client wrapper re-parse + the landing 12-file type sweep, `GET /api/v1/gallery` (assembled `{categories, photos}` — active-only, position-ordered, uncategorized absent) + `GET /api/v1/testimonials` with typed wrappers, branches public read active-only (gated appointment reads untouched — MUST-NOT-TOUCH held), deactivated-slug uniform 404 pinned; api 262→278 (+3 gated skips). Classifier SPLIT — 26 v1-paths + 7 main-only (the 4 booking lib files = DU; docs/plan/progress/superpowers-plan dropped). Content pass: AGENTS.md content-dropped (#145/#146 ruling); package-card.tsx + typing.test.ts resolved to v1's own variants with ONLY the type swap (booking-affordance hunks stay out per the landing booking-off class); the branches appointment-resolution test DROPPED (substance = the /api/v1/appointments POST + resend seam, absent endpoints on v1 — AC-3 is main-only; caught at the first gate run: @sevendays/api#test failed); the plan doc's clean-add removed (the #146 lesson recurring); one biome blank-line fix. Locks: install/build:packages/api-build green, check 35/35 + build 7/7, audit PASS 0/18; run (v1 push of ebe85f6) `check` + `Deploy v1 (private)` success, `Deploy teaser (main)` skipped. Deferred minors ledgered: gallery/testimonial wrapper loopback tests → #139/#142 planners; AR7's testimonial-half unpinned (falls out trivially). |
