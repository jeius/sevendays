# ADR-XXXX: <short, decision-oriented title>

**Status:** Proposed | Accepted | Superseded by ADR-XXXX | Deprecated
**Date:** YYYY-MM-DD

## Context

What's the situation that forces a decision? What constraints, requirements, or forces are in tension? Write this so that someone with no memory of the discussion (including future-you) understands *why this was even a question*.

## Decision

What are we doing. State it plainly, one or two sentences if possible. This is the part that gets referenced from code comments, PRs, and project docs.

## Alternatives Considered

- **Option A** — why it was rejected (or deferred)
- **Option B** — why it was rejected (or deferred)

Only list alternatives that were seriously considered, not a strawman list.

## Consequences

What gets easier, what gets harder, what new obligations does this create (ops burden, follow-up decisions, things to revisit later). Include the honest trade-offs, not just the upside — that's the point of writing this down instead of just doing it.

---

*Keep ADRs short — a page, not an essay. If a decision changes, don't edit this file in place; write a new ADR that supersedes it and update this one's Status line. The history of "we used to do X, then switched to Y because Z" is itself useful information.*
